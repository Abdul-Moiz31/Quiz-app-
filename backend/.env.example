# Port the server listens on (deploy platforms set this automatically)
PORT=3000

# Password hosts must enter on the /admin screen
ADMIN_PASSWORD=ADMIN_PASSWORD

# Allowed CORS origin for the Socket.io connection ("*" = any)
CORS_ORIGIN=*
