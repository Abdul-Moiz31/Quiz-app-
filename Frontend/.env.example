# Backend URL the frontend connects to.
# Leave unset to use same-origin in production and http://localhost:3000 in dev.
# Set this only if you deploy the frontend separately from the backend.
# VITE_SERVER_URL=https://your-backend.onrender.com
